{% comment %}
  The `contact-section` is a customizable section for displaying contact information, policies, or FAQ content.
  It includes a sidebar for navigation and dynamic content blocks.
{% endcomment %}
{{ 'content--contact.css' | asset_url | stylesheet_tag }}

<div class="faq-section">
  <div class="faq-container">
    <div class="faq-header">
      <div class="faq-title-line"></div>
      <h1 class="faq-title">{{ section.settings.page_title | default: "CONTACT" }}</h1>
      <div class="faq-title-line"></div>
    </div>
    <div class="faq-content">
      <aside class="faq-sidebar">
        <ul>
          {% if section.settings.link_1_text != blank %}
            <li>
              <a href="{{ section.settings.link_1_url }}">{{ section.settings.link_1_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_2_text != blank %}
            <li>
              <a href="{{ section.settings.link_2_url }}">{{ section.settings.link_2_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_3_text != blank %}
            <li>
              <a href="{{ section.settings.link_3_url }}">{{ section.settings.link_3_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_4_text != blank %}
            <li>
              <a href="{{ section.settings.link_4_url }}">{{ section.settings.link_4_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_5_text != blank %}
            <li>
              <a href="{{ section.settings.link_5_url }}">{{ section.settings.link_5_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_6_text != blank %}
            <li>
              <a href="{{ section.settings.link_6_url }}">{{ section.settings.link_6_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_7_text != blank %}
            <li>
              <a href="{{ section.settings.link_7_url }}">{{ section.settings.link_7_text }}</a>
            </li>
          {% endif %}
          {% if section.settings.link_8_text != blank %}
            <li>
              <a href="{{ section.settings.link_8_url }}">{{ section.settings.link_8_text }}</a>
            </li>
          {% endif %}
        </ul>
      </aside>
      <main class="faq-main">
        <div class="contact-form-wrapper">
          {%- comment -%}
            "CONTACT US" above is this page's heading. This one sits beneath it
            and ships empty in every region, so as an h1 it was a second, blank
            main heading. As an h2 it is a subheading, and .contact-main-heading
            sets its own font-size, so nothing about it renders differently.
          {%- endcomment -%}
          <h2 class="contact-main-heading">{{ section.settings.main_heading | default: "" }}</h2>
          <div class="contact-description">
            {{ section.settings.main_description }}
          </div>

          {%- form "contact", id: "ContactForm", class: "contact-form" -%}
            {%- if form.posted_successfully? -%}
              <p class="form-status form-status-list form__message" tabindex="-1" autofocus>
                {{ "templates.contact.form.post_success" | t }}
              </p>
            {%- elsif form.errors -%}
              <div class="form__message">
                <h2 class="form-status caption-large text-body" role="alert" tabindex="-1" autofocus>
                  {{ "templates.contact.form.error_heading" | t }}
                </h2>
              </div>
            {%- endif -%}

            <div class="contact-form-field">
              <label for="ContactForm-name">First name</label>
              <input
                type="text"
                id="ContactForm-name"
                name="contact[name]"
                value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
                placeholder="First name">
            </div>

            <div class="contact-form-field">
              <label for="ContactForm-email">Email</label>
              <input
                autocomplete="email"
                type="email"
                id="ContactForm-email"
                name="contact[email]"
                spellcheck="false"
                autocapitalize="off"
                value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                aria-required="true"
                {% if form.errors contains "email" %}
                  aria-invalid="true"
                  aria-describedby="ContactForm-email-error"
                {% endif %}
                placeholder="Email"
                required>
            </div>

            <div class="contact-form-field">
              <label for="ContactForm-phone">Phone number</label>
              <input
                type="tel"
                id="ContactForm-phone"
                autocomplete="tel"
                name="contact[phone]"
                pattern="[0-9\-]*"
                value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
                placeholder="Your phone number (optional)">
            </div>

            <div class="contact-form-field">
              <label for="ContactForm-body">Message</label>
              <textarea
                rows="10"
                id="ContactForm-body"
                name="contact[body]"
                placeholder="Enter your message">
                {{- form.body -}}
              </textarea>
            </div>

            <div class="contact-form-submit">
              <button type="submit" class="contact-submit-btn">SEND</button>
            </div>
          {%- endform -%}
        </div>
      </main>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const sidebarLinks = document.querySelectorAll('.faq-sidebar a');
    const currentPath = window.location.pathname;
    const normalizedCurrentPath = currentPath === '/' ? '/' : currentPath.replace(/\/$/, '');
    let foundActive = false;
    sidebarLinks.forEach(link => {
      if (foundActive) return;
      const linkPath = new URL(link.href).pathname;
      const normalizedLinkPath = linkPath === '/' ? '/' : linkPath.replace(/\/$/, '');
      if (normalizedLinkPath === normalizedCurrentPath) {
        link.classList.add('active');
        foundActive = true;
      }
    });
  });
</script>

{% schema %}
{
  "name": "Contact Section",
  "settings": [
    {
      "type": "text",
      "id": "page_title",
      "label": "Page Title",
      "default": "CONTACT"
    },
    {
      "type": "text",
      "id": "main_heading",
      "label": "Main Content Heading",
      "default": "Contact Us"
    },
    {
      "type": "richtext",
      "id": "main_description",
      "label": "Main Content Description",
      "default": "<p>If, for any reason, you have any issues or simply just have some questions for us, please do not think twice about contacting our support team at;</p><p>help@theessencevault.co.uk</p><p>We aim to respond to all questions within 24 hours.</p><p>Speak soon!</p>"
    },
    {
      "type": "text",
      "id": "link_1_text",
      "label": "Link 1 Text",
      "default": "[ CONTACT US ]"
    },
    {
      "type": "url",
      "id": "link_1_url",
      "label": "Link 1 URL"
    },
    {
      "type": "text",
      "id": "link_2_text",
      "label": "Link 2 Text",
      "default": "FAQS"
    },
    {
      "type": "url",
      "id": "link_2_url",
      "label": "Link 2 URL"
    },
    {
      "type": "text",
      "id": "link_3_text",
      "label": "Link 3 Text",
      "default": "STOCKISTS"
    },
    {
      "type": "url",
      "id": "link_3_url",
      "label": "Link 3 URL"
    },
    {
      "type": "text",
      "id": "link_4_text",
      "label": "Link 4 Text",
      "default": "PRIVACY POLICY"
    },
    {
      "type": "url",
      "id": "link_4_url",
      "label": "Link 4 URL"
    },
    {
      "type": "text",
      "id": "link_5_text",
      "label": "Link 5 Text",
      "default": "TERMS AND CONDITIONS"
    },
    {
      "type": "url",
      "id": "link_5_url",
      "label": "Link 5 URL"
    },
    {
      "type": "text",
      "id": "link_6_text",
      "label": "Link 6 Text",
      "default": "REFUND POLICY"
    },
    {
      "type": "url",
      "id": "link_6_url",
      "label": "Link 6 URL"
    },
    {
      "type": "text",
      "id": "link_7_text",
      "label": "Link 7 Text"
    },
    {
      "type": "url",
      "id": "link_7_url",
      "label": "Link 7 URL"
    },
    {
      "type": "text",
      "id": "link_8_text",
      "label": "Link 8 Text"
    },
    {
      "type": "url",
      "id": "link_8_url",
      "label": "Link 8 URL"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Contact Section",
      "blocks": []
    }
  ]
}
{% endschema %}
