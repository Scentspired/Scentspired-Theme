{% comment %}
  Dual Image Slider Section – Final Mobile Padding Fix
{% endcomment %}

<style>
    @font-face {
      font-family: 'PP Mori';
      src: url('{{ "PPMori-Regular.woff2" | asset_url }}') format('woff2');
      font-weight: 400;
      font-style: normal;
    }

    .dual-slider-wrapper {
      max-width: 2100px;
      width: 100%;
      margin: 0 auto:
      padding: 40px 0px;
      box-sizing: border-box;
      text-align: center;
      padding-left: 5rem;
      padding-right: 5rem;
    }

    .desktop-slider {
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    .desktop-slider .image-box {
      flex: 1;
      max-width: 50%;
    }

    .desktop-slider img {
      width: 100%;
      border-radius: 12px;
      height: auto;
      object-fit: cover;
      display: block;
    }

    .slider-caption {
      font-family: 'PP Mori', sans-serif;
      font-weight: 400;
      font-size: 17px;
      max-width: 900px;
      margin: 40px auto 0;
      line-height: 1.6;
      color: #000;
    }

    .mobile-slider {
      display: none;
      position: relative;
      width: 100%;
      overflow-x: hidden;
      margin: 0 auto;
    }

    .mobile-slide-wrapper {
      width: 100%;
      box-sizing: border-box;
      padding-inline: 16px; /* ✅ Equal left and right padding */
    }

    .mobile-slide {
      display: none;
      width: 100%;
      text-align: center;
    }

    .mobile-slide.active {
      display: block;
    }

    .mobile-slide img {
      width: 100%; /* ✅ Fully fills the padded container */
      height: auto;
      border-radius: 12px;
      display: block;
    }

    .slider-arrow {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: yellow;
      color: black;
      font-size: 24px;
      padding: 2px 12px;
      border: none;
      cursor: pointer;
      z-index: 5;
      border-radius: 999px;
      display: none;
    }

    .slider-arrow.left {
      left: 20px;
    }

    .slider-arrow.right {
      right: 20px;
    }

    @media (max-width: 768px) {
      .desktop-slider {
        display: none;
      }

      .mobile-slider {
        display: block;
      }

      .slider-caption {
        font-size: 20px;
        padding: 0 16px;
        width: 300px;
        margin-left: 0px;
  }
    }
     @media screen and (min-width: 413px) and (max-width: 415px) {

  .mobile-slider{
     margin-left: -40px !important;
     width: 125% !important;
  }
  .right{
   margin-right: 0px !important;
  }
  }
  @media screen and (min-width: 387px) and (max-width: 392px) {
  .mobile-slider{
   margin-left: -41px !important;
  }
  }
  @media screen and (min-width: 427px) and (max-width: 432px) {
  .mobile-slider{
   margin-left: -90px !important;
   width: 180% !important;
  }
  .slider-caption {
        font-size: 20px;
        padding: 0 16px;
        width: 300px;
        margin-left: -30px !important;
  }
  }
  @media screen and (min-width: 410px) and (max-width: 412px) {
  .mobile-slider{
   margin-left: -40px !important;
   width: 126% !important;
  }
  }
  @media screen and (min-width: 765px) and (max-width: 770px) {
  .mobile-slider{
   margin-left: -40px !important;
   width: 120% !important;
  }
    .slider-caption {
        font-size: 20px;
        padding: 0 16px;
        width: 500px;
        margin-left: 40px !important;
  }
  }
  @media screen and (min-width: 535px) and (max-width: 545px) {
  .mobile-slider{
   margin-left: -40px !important;
   width: 130% !important;
  }
  .slider-caption {
        font-size: 20px;
        padding: 0 16px;
        width: 300px;
        margin-left: 0px !important;
  }
  }
</style>

<div class="dual-slider-wrapper">
  <!-- Desktop View -->
  <div class="desktop-slider">
    <div class="image-box">
      {% if section.settings.image_1 != blank %}
        <img src="{{ section.settings.image_1 | image_url: width: 1200 }}" alt="Image 1">
      {% endif %}
    </div>
    <div class="image-box">
      {% if section.settings.image_2 != blank %}
        <img src="{{ section.settings.image_2 | image_url: width: 1200 }}" alt="Image 2">
      {% endif %}
    </div>
  </div>

  <!-- Mobile View -->
  <div class="mobile-slider">
    <button class="slider-arrow left">‹</button>
    <button class="slider-arrow right">›</button>

    <div class="mobile-slide-wrapper">
      {% if section.settings.image_1 != blank %}
        <div class="mobile-slide active">
          <img src="{{ section.settings.image_1 | image_url: width: 800 }}" alt="Image 1">
        </div>
      {% endif %}
    </div>
  </div>

  <h2 class="main-heading">Inspired by Icons, Crafted Independently</h2>
  <div class="slider-caption">
    The world of fragrance is rich with legendary houses and iconic creations that continue to inspire perfumers
    globally. At Scentspired, these serve as creative reference points, guiding our understanding of balance, structure,
    and emotion.
    <br>
    <br>
    Each Scentspired fragrance is an independent formulation, carefully developed to capture the essence of admired
    scent styles while expressing its own character. We are not affiliated with or endorsed by any designer or luxury
    brand. Our approach is rooted in respect for the icons that came before us, and for the individuality of the person
    wearing our fragrance.
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const slides = document.querySelectorAll('.mobile-slide');
    const leftArrow = document.querySelector('.slider-arrow.left');
    const rightArrow = document.querySelector('.slider-arrow.right');
    let current = 0;

    function showSlide(index) {
      slides.forEach((s, i) => s.classList.toggle('active', i === index));
    }

    if (leftArrow && rightArrow) {
      leftArrow.addEventListener('click', function () {
        current = (current - 1 + slides.length) % slides.length;
        showSlide(current);
      });

      rightArrow.addEventListener('click', function () {
        current = (current + 1) % slides.length;
        showSlide(current);
      });
    }

    showSlide(current);
  });
</script>

{% schema %}
{
  "name": "Dual Image Slider",
  "settings": [
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "First Image"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Second Image"
    }
  ],
  "presets": [
    {
      "name": "Dual Image Slider"
    }
  ]
}
{% endschema %}
