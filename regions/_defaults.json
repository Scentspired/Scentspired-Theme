{
  "$comment": "Neutral baseline. This file holds no storefront identity: a region that omits its domain, currency, addresses or locale fails validation rather than silently inheriting another region. Defaults here are the safe answer, not the USA answer.",
  "is_default": false,
  "published": false,
  "company_address": "",
  "trustpilot": {
    "enabled": false
  }
}
