/* The addresses page: starts Dawn's CustomerAddresses (assets/account--customer.js). Moved from sections/account--addresses.liquid. */
  window.addEventListener('load', () => {
    typeof CustomerAddresses !== 'undefined' && new CustomerAddresses();
  });
