{%- comment -%}
  GENERATED — do not edit. Empty on purpose: the theme holds no content. Each region's build generates this from regions/<id>/content/*.json
  Usage: render 'region--content' with key set to '<page>.<section>.<field>' (add js: true inside a JS string)
{%- endcomment -%}
{%- assign content_page = key | split: '.' | first -%}
{%- case content_page -%}

{%- endcase -%}
