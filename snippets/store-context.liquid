{%- comment -%}
  ============================================================================
  SCENTSPIRED — Centralized Store Context Facade & Registry
  ============================================================================
  Single Source of Truth for all regional metadata, settings, and feature flags.
  Delegates dynamically to the active regional strategy module.

  Modes:
  1. Key Accessor: {% render 'store-context', key: 'property_name' %}
     Outputs the exact scalar property, bundle fallback, or feature flag string.
  2. Full JSON Serializer: {% render 'store-context' %}
     Outputs a valid JSON object literal for JavaScript consumption.
  ============================================================================
{%- endcomment -%}
{%- render 'region-strategy', key: key -%}
