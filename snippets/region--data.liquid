{%- comment -%}
  GENERATED — do not edit. Empty on purpose: the theme holds no data. Each region's build generates this from regions/<id>/data/*.json
  Usage: render 'region--data' with name set to a data file's name, without .json
{%- endcomment -%}
{%- case name -%}

  {%- else -%}null
{%- endcase -%}
